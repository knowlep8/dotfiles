dbus_addr_file="$HOME/.dbus_addr"

if [ -n "$DBUS_SESSION_BUS_ADDRESS" ]; then
    echo "$DBUS_SESSION_BUS_ADDRESS" > $dbus_addr_file
elif [ -e $dbus_addr_file ]; then
    export DBUS_SESSION_BUS_ADDRESS=`cat $dbus_addr_file`
fi
